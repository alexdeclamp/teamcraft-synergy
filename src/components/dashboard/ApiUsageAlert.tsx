
import React from 'react';
import { useUserFeatures } from '@/hooks/useUserFeatures';

// API limitations have been removed, so we don't need to show any alerts
const ApiUsageAlert = () => {
  return null;
};

export default ApiUsageAlert;
